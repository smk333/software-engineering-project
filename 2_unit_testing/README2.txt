In order to run the unit testing for Blockchain.js, open up the Command Prompt on a Windows machine. 
If npm is not already installed, install it by typing in the following command:
npm install

Once npm has been installed, go into the directory that contains the Blockchain.js file by typing in:
cd js-blockchain

After that, run the unit tests through the following command:
node Blockchain.js
